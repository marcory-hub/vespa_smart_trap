---
description: Repository Map & Context Orchestration. Defines which files contain the "Source of Truth" for specific topics.
alwaysApply: true
---
# PROJECT CONTEXT: vespa_smart_trap
**Focus:** YOLO11n on GV2 + T-SIM for Asian hornet detection.

## 1. Context loading
`notes/` is gitignored (local Obsidian sync). Use the exact paths below; codebase search may not list them. If `Read` fails, stop and ask the user to sync (`notes/_cli_vst.md`); do not guess or invent a substitute path.

Before architecture or embedded suggestions, follow `.cursor/skills/read-notes/SKILL.md` and read the relevant note:
- Index: `notes/__vespa_smart_trap.md`
- CLI / Obsidian sync: `notes/_cli_vst.md`
- Hardware / pinouts / UART: `notes/_hardware_vst.md`
- Roadmap: `notes/_timeline_vst.md`
- Models: `notes/_model_vst.md`
- Layout: `notes/_project_layout_vst.md`
- Training notebooks (public): GitHub `marcory-hub/Seeed_Grove_Vision_AI_Module_V2` (`main`); direct links in `README.md` only.
- Training notebooks (agent local): read-only `notebooks/*.ipynb` (gitignored; `.cursorignore` negation); cite cells from Read or GitHub raw. Do not document local mirror in `README.md`.
- Scripts / utilities: `scripts/script-index.md`, then search `scripts/` before proposing a new tool

## 2. Source of Truth Matrix

| Topic | Pointer |
| :--- | :--- |
| Notes (mandatory Read) | `.cursor/skills/read-notes/SKILL.md` |
| Datasets / labels | `.cursor/skills/dataset/SKILL.md` |
| Colab train / VELA | `.cursor/skills/train-colab-vela/SKILL.md` |
| GCE VELA run (agent terminal) | `.cursor/commands/gce-run` + `scripts/gce_vela/remote.sh` |
| GCE Jupyter + tunnel | `.cursor/commands/gce-jupyter` + `.cursor/skills/gce-jupyter/SKILL.md` |
| GCE start / stop / cost audit | `.cursor/commands/gce-start`, `gce-stop`, `gce-status` + `.cursor/skills/gce-ops/SKILL.md` |
| GCE budget auto-stop | `.cursor/commands/gce-budget-kill` + `scripts/gce_vela/budget_kill_switch/` |
| Flash GV2 (run) | `.cursor/commands/flash-gv2` |
| Flash GV2 (when/verify) | `.cursor/skills/flash-gv2/SKILL.md` |
| UART / serial debug | `.cursor/skills/uart-debug/SKILL.md` |
| Model test / benchmark | `.cursor/skills/model-test/SKILL.md` |
| Project pitch | `.cursor/skills/project-pitch/SKILL.md` |
| Presentation deck | `.cursor/skills/presentation/SKILL.md` (`presentations/edge-ai-hornet-detector-2026-06.md`; hard gate) |
| README voice | `.cursor/skills/update-readme/SKILL.md` |
| Submodule push | `.cursor/commands/push-submodule` |
| Notebooks (local) | `notebooks/` + `notebooks/AGENTS.md` (read-only) |
| Teach-me workspace | `docs/teaching/` + `.cursor/skills/teach-me/SKILL.md` |
| Session handoff | `.cursor/skills/handoff/SKILL.md` → `sessions/latest.md` |
| Research / build prompt | `.cursor/skills/research-gaps/`, `.cursor/skills/build-prompt/`, `@grill-me` |
| Token efficiency | `.cursor/skills/less-tokens/SKILL.md` |
| Make skill | `.cursor/skills/make-skill/SKILL.md` |
| Shell safety hooks | `.cursor/hooks.json` → `guard-shell.sh` |
| Dataset review subagent | `.cursor/agents/dataset-guard.md` |
| Embedded review subagent | `.cursor/agents/gv2-embedded-reviewer.md` |
| Presentation review subagent | `.cursor/agents/presentation-steward.md` |
| Security audit | `.cursor/commands/security-audit` |

## 3. Sync
After a `@teach-me` session changes project facts: ask user to update `notes/` or rules. Rules (how) > notes (what), unless user intent overrides outdated `_timeline_vst.md`.

## 4. Pipeline
GV2 inference → UART → T-SIM LTE. Pinouts, baud, SRAM limits: `notes/_hardware_vst.md`.

## 5. File integrity
- Notes: agent suggests; user writes unless "Apply to file." Do not publish note content unless user asks.
- **Dataset safety:** never rename `.jpg` or `.txt` label files.
- Destructive ops: `safety-guardrails.mdc`.

## 6. Do not suggest
- **Platforms:** RPi detector, ESP-IDF-only, XIAO-only (no LilyGO), inference on LilyGO ESP32-S3, standard Grove shields.
- **Stack:** GV2 + LilyGO T-SIM7080G-S3; ESP32-S3 test only. GV2 sole inference engine.
- **Training:** No local train/quant/VELA; Colab `2025.07`, Python 3.11. No `int8.tflite`; use `int8_vela.tflite`. No YOLOv26. Resolution per `notes/_hardware_vst.md`.
- **Hygiene:** No empty/placeholder notes.
